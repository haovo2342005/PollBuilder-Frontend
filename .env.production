# This value is only a local fallback for `npm run build` on your machine.
# On Vercel, set VITE_API_BASE_URL as a real Environment Variable in the
# project settings instead of editing this file — that is what the
# deployed build actually uses.
VITE_API_BASE_URL=https://REPLACE-WITH-YOUR-RENDER-GATEWAY-URL.onrender.com
